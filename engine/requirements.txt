# climatePrep compute engine — M0 (lean). Pinned to match the WSC service.
# The scientific stack (numpy/scipy/lmoments3/pandas/statsmodels) is added at M3
# and MUST be pinned to the WSC versions for zero behavioural drift (spec §3.2).
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4

# Editable install of the numerical core package.
-e ../packages/core-engine

# Dev/test
pytest==8.4.2
httpx==0.28.1
