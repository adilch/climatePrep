# climatePrep compute engine. Pinned to match the WSC service exactly
# (zero behavioural drift, spec §3.2). lmoments3 joins at M3 (PFA).
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
numpy==2.4.6
scipy==1.17.1
lmoments3==1.0.8
matplotlib==3.11.0

# Editable install of the numerical core package.
-e ../packages/core-engine

# Dev/test
pytest==8.4.2
httpx==0.28.1
